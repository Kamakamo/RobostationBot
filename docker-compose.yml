version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: RobostationBot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./bot_data.pickle:/app/bot_data.pickle